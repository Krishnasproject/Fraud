# Core packages
gunicorn==21.2.0
Flask==2.3.2
Flask-Login==0.6.2
Flask-SQLAlchemy==3.1.1
Werkzeug==2.3.7
python-dotenv==1.0.0
requests==2.31.0
SQLAlchemy==2.0.20
psycopg2-binary==2.9.9

# Data science stack
# Using numpy 2.0.2 as pickle files were created with numpy 2.x (requires numpy._core)
# Note: pandas 2.2.x requires numpy<2, so we'll let pip find compatible versions
# If this fails, pickle files need to be regenerated with numpy 1.x
numpy==2.0.2
pandas
scikit-learn==1.5.0
joblib==1.3.2

# Build requirements
setuptools==68.0.0
wheel==0.41.2
cython==0.29.36
Flask-Limiter==3.5.0
Flask-Mail==0.9.1
